package com.pdfjet;

class TextOffset {
    protected String text;
    protected float offset;

    protected TextOffset(String text, float offset) {
        this.text = text;
        this.offset = offset;
    }
}
